@require "common"
@require "flex"
@require "textFormat"

@require "slide"
@require "question"

@require "audio"
@require "video"

@require "freeText"
@require "kacPS"
@require "likertScale"
@require "oneDScale"
@require "audioInformationRetrieval"
@require "tagging"
@require "soloStimulus"

@require "webgazer"

@require "radioButtonGroup"

body {
	padding-top: 20px;
}

.HalfColumn {
	width: 12.5% !important;
}

.swal2-content {
  font-size: 1.8em !important;
  font-weight: 300;
}

.swal2-content > div {
  text-align: left;
}

ul.calibrate-instructions {
  text-align: left;
}

.swal2-content > div > ul > li,
ul.calibrate-instructions > li {
  padding-left: 10px !important;
}

.soto-voce {
  font-size: 1.2rem;
}

.flex {
  display: flex;
}

.flex-col {
  flex-direction: column;
}