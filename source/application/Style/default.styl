@require "common"
@require "flex"
@require "textFormat"

@require "slide"
@require "question"

@require "audio"
@require "video"

@require "freeText"
@require "kacPS"
@require "likertScale"
@require "oneDScale"
@require "audioInformationRetrieval"
@require "tagging"
@require "soloStimulus"

@require "webgazer"
@require "landmarker"

@require "radioButtonGroup"

body {
	//padding-top: 20px;
}

.HalfColumn {
	width: 12.5% !important;
}

.swal2-content {
  font-size: 1.8em !important;
  font-weight: 300;
}

.swal2-content > div {
  text-align: left;
}

ul.calibrate-instructions {
  text-align: left;
}

.swal2-content > div > ul > li,
ul.calibrate-instructions > li {
  padding-left: 10px !important;
}

.soto-voce {
  font-size: 1.2rem;
}

.flex {
  display: flex;
}

.flex-col {
  flex-direction: column;
}

// footers

html, body {
  height: 100%;
}
.shell {
  height: 100%;
  min-height: 100%;
}
.footer-push {
  height: 40px;
}

.shell-body {
  margin-bottom: -40px;
  min-height: 100%;
}

.shell-footer {
  height: 24px;
}

.panel {
  display: flex;
  flex-direction: column;
  height: 100%;

  > .panel-body {
    flex-grow: 1;
  }

  > .panel-footer {
    padding: 0 10px 8px 10px;

    .slide-nav-right {
      display: flex;
      align-items: center;
      justify-content: space-evenly;

      > .slide-position-details {
        > span {
          //margin: 0 4px;
        }
        margin-right: 16px;
        vertical-align: middle;
      }
    }
  }
}
:root {
  --max-width-small: 575.98px;
  --max-width-medium: 767.98px;
  --max-width-large: 991.98px;
  --max-width-xlarge: 1199.98px;
  --max-width-xxlarge: 1399.98px;
  --max-width-custom: 1000px;
}

body {
  font-size: var(--font-size-medium);
  padding: var(--padding-medium);
}

/* Extra small devices (phones, less than 576px) */
@media (max-width: var(--max-width-small)) {
  .slide-content {
    width: 400px;
  }
}

/* Small devices (landscape phones, 576px and up) */
@media (max-width: var(--max-width-medium)) {
  .slide-content {
    width: 600px;
  }
}

/* Medium devices (tablets, 768px and up) */
@media (max-width: var(--max-width-large)) {
}

/* Large devices (desktops, 992px and up) */
@media (max-width: var(--max-width-xlarge)) {
}
/* Extra large devices (large desktops, 1200px and up) */
@media (max-width: var(--max-width-xxlarge)) {
}

.slide-content {
  width: 800px;
  margin: 0 auto;
}